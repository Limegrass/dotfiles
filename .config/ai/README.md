# AI

## Hooks

I believe format/events differs per client, needs individual setup/a good way to figure that out.

- Caveman does not activate consistently. Usually requires setting up hook per client.
- Date time injection is helpful.
- Linters/formatting hooks per client
- Notifying completion/ready for user input through notify-send/tmux
